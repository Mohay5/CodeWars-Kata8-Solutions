// Create a Function that takes an integer as an argument and returns "Even"for even numbers or "Odd" for add numbers.

const EvenOdd = value => value % 2 == 0 ? "Even" : "Odd"
let nada = EvenOdd(10)
console.log(nada)